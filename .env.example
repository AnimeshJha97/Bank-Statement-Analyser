DEPLOYMENT_MODE=self-hosted
DATABASE_URL=postgres://postgres:postgres@localhost:5432/statement
# Generate once with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=
# Optional global fallback. Prefer a per-user key in Settings.
OPENAI_API_KEY=
NEXT_TELEMETRY_DISABLED=1
